import { setupFormHandlers } from './src/formHandlers.js';

// Inicializar los manejadores de formulario cuando se carga el documento
document.addEventListener('DOMContentLoaded', setupFormHandlers);
